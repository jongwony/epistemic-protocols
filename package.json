{
  "name": "epistemic-protocols-hooks",
  "version": "0.0.0",
  "private": true,
  "description": "Husky-managed pre-commit hooks for runtime-contract verification. Plugin code itself uses Node.js stdlib only.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "commonmark": "^0.31.2",
    "husky": "^9.1.7"
  }
}
