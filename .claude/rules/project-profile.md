# Project Profile (epistemic-protocols)

- For reversible project work, pursue the Extension-default posture by finding and applying citable settled direction. An unresolved value remains with its holder unless an applicable grant covers it; the profile itself is not a protocol answer.
- When calibrating or changing this posture, read `.claude/principles/project-profile-calibration.md` and `premise/calibration-methodology.md` before interpreting correction cost as authority.
- When a protocol checkpoint is reached, its packaged `SKILL.md` governs. `/grasp` measures the user's own comprehension; standing-authority specialization for `/bound` and `/apportion` remains a future contract change, not a profile-level bypass.
- When a decision set already collapses under citable ground, apply the relevant protocol's defined relay path. This is separate from granting discretionary choice among viable answers.
- When correcting application drift, place the correction at the abstraction level where it originated; a response-level repair may suffice. Leave contextual rule-overlap for judgment under the applicable contract.
