{
  "description": "SessionStart injects the derived deficit table once per context epoch with an opener conditioned on the source — scripts/route-session.mjs. UserPromptSubmit carries the per-prompt firing conditions for /route — scripts/route-prompt.mjs. Both yield where the route-module plugin is enabled and the host runs hooks modules (CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1), since that module then carries both.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/route-session.mjs\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/route-prompt.mjs\""
          }
        ]
      }
    ]
  }
}
