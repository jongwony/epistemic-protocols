{
  "description": "Two hooks. SessionStart injects the derived deficit table once per context epoch (startup, resume, clear, compact) with an opener conditioned on the source, and the premise index beneath it with every path absolute \u2014 scripts/route-session.mjs. UserPromptSubmit carries the per-prompt firing conditions for /route \u2014 scripts/route-prompt.mjs. Only `description` and `hooks` go in this file: Codex rejects any other top-level key and then registers none of these hooks.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/route-session.mjs\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/route-prompt.mjs\""
          }
        ]
      }
    ]
  }
}
